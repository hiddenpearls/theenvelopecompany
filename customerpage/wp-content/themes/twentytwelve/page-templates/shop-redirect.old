<?php
/**
 * Template Name: Shop Redirect
 *
 * Description: Twenty Twelve loves the no-sidebar look as much as
 * you do. Use this page template to remove the sidebar from any page.
 *
 * Tip: to remove the sidebar from all posts and pages simply remove
 * any active widgets from the Main Sidebar area, and the sidebar will
 * disappear everywhere.
 *
 * @package WordPress
 * @subpackage Twenty_Twelve
 * @since Twenty Twelve 1.0
 */
?>
<?php
global $current_user;
get_currentuserinfo();

$get_username_directory = $current_user->nickname;
$page_title = $wp_query->post->post_slug;

if($page_title != $get_username_directory){
    header('Location: http://az.youareheremedia.com/product/'.$get_username_directory); 
/*	echo ($url);
	echo ($get_username_directory); */
    exit;
}
else {
    header('Location: http://az.youareheremedia.com/my-account/');
    exit;
}
?>